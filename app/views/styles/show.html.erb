<%= render @style %>

<div>
  <%= link_to "Edit this style", edit_style_path(@style) if current_user %> |
  <%= link_to "Back to styles", styles_path %>

  <%= button_to "Destroy this style", @style, method: :delete, class:"btn btn-danger" if current_user %>
</div>
