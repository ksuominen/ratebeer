<div id="<%= dom_id beer %>">
  <p>
    <strong>Name:</strong>
    <%= beer.name %>
  </p>

  <p>
    <strong>Style:</strong>
    <%= beer.style %>
  </p>

  <p>
    <strong>Brewery:</strong>
    <%= beer.brewery.name %>
  </p>

  <p>
    <% if beer.ratings.empty? %>
      beer has not yet been rated!
    <% else %>
      Beer has <%= beer.ratings.count %> ratings with an average of <%= beer.ratings.average(:score) %>
    <% end %>
  </p>

</div>
